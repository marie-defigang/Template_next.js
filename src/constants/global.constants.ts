export enum KEY_CODES {
  ENTER = 13,
  SPACE = 32,
}

export enum SORT_DIRECTIONS {
  DESC = 'DESC',
  ASC = 'ASC',
}

export const ESCAPE_SPECIAL_CHARACTERS_REGEXP = /[-[\]{}()*+?.,\\^$|#\s]/g;

export const DEBOUNCE_DELAY = 700;
